# Plan: CRO Review - Hero Image and Mobile/Desktop Layout

## Assumptions
- New hero image (Pexels 261327, infinity pool) was added in this branch
- Deployed site at https://poollogic-astro.vercel.app/ reflects main branch (brand-blue gradient)
- This branch accidentally reverted the gradient from brand-blue back to gray-900

## Acceptance Criteria
1. Hero gradient uses brand blue (#00568c) per task specification: from-[#00568c]/80 via-[#00568c]/50 to-transparent
2. Hero image fills full viewport width on both 375px and 1440px (no gaps)
3. Both CTAs visible above the fold on 375px mobile (verified: CTAs at 703px < 740px effective viewport)
4. No horizontal overflow on body at 375px (verified: body scrollWidth = 375px)
5. Mobile hamburger button >= 44px tap target (fixed: p-2.5 + min-h/w 44px)
6. Mobile Service Areas accordion trigger >= 44px tap target (fixed: py-3 min-h-44px)
7. City service area links >= 44px tap target on mobile (fixed: py-2.5 block)
8. Build passes with 0 errors

## Approach
1. Write the plan (this file)
2. Take screenshots at 375px and 1440px to assess current state
3. Identify CRO issues
4. Fix gradient overlay (gray-900 → brand blue #00568c) + restore related regressions
5. Fix header tap targets (hamburger, accordion trigger)
6. Fix city links tap targets
7. Build and verify no errors
8. Commit and create PR

## Progress
- [x] Create plan (2026-04-10)
- [x] Take mobile/desktop screenshots (2026-04-10)
- [x] Restore brand-blue gradient (from-[#00568c]/80 via-[#00568c]/50 to-transparent) + bottom overlay
- [x] Restore object-center on hero image
- [x] Restore min-h-[500px] on mobile hero
- [x] Restore widths=[640, 1280, 1920] for better image quality
- [x] Fix mobile hamburger tap target (p-2.5 + min-h/w 44px)
- [x] Fix mobile accordion trigger tap target (py-3 min-h-44px)
- [x] Fix city service area links tap target (py-2.5 block)
- [x] Build verified clean (224 pages, 0 errors) (2026-04-10)
- [ ] Commit changes
- [ ] Create PR

## Decision Log
- Restored dual overlay (brand-blue L-R + black/30 T-B bottom) from main branch: the bottom overlay adds depth and helps CTA readability at the bottom of the hero
- Kept min-h-[500px] (restored from 480px): provides enough room for all hero content without cramping on small phones
- Used widths=[640, 1280, 1920]: better quality at common tablet/laptop sizes vs 1024

## Surprises & Discoveries
- The deployed site (main branch) already had the brand-blue gradient; this branch had accidentally reverted it to gray-900
- The new Pexels 261327 image is an infinity pool with palm trees and rolling hills - bright/aspirational, pairs well with brand blue gradient
- Body scrollWidth stays at 375px on mobile; comparison table overflow is intentional (overflow-x-auto wrapper)
- CTAs in hero land at 703px bottom on 375px/812px viewport; sticky CTA is 72px = 740px effective, CTAs safely above fold
