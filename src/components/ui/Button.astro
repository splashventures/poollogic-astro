---
import { cn } from '@/lib/utils';

interface Props {
  variant?: 'primary' | 'secondary' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  href?: string;
  external?: boolean;
  class?: string;
  type?: 'button' | 'submit' | 'reset';
  disabled?: boolean;
}

const {
  variant = 'primary',
  size = 'md',
  href,
  external,
  class: className,
  type = 'button',
  disabled,
} = Astro.props;

const variantStyles: Record<string, string> = {
  primary: 'bg-brand-blue text-white hover:bg-brand-blue-light focus:ring-brand-blue shadow-sm',
  secondary: 'bg-brand-sky text-white hover:bg-brand-sky-light focus:ring-brand-sky shadow-sm font-bold',
  outline: 'border-2 border-brand-blue text-brand-blue hover:bg-brand-blue hover:text-white focus:ring-brand-blue',
  ghost: 'text-brand-blue hover:bg-brand-sky/10 focus:ring-brand-blue',
};

const sizeStyles: Record<string, string> = {
  sm: 'px-3 py-1.5 text-sm',
  md: 'px-5 py-2.5 text-base',
  lg: 'px-8 py-3.5 text-lg',
};

const baseStyles = cn(
  'inline-flex items-center justify-center font-semibold rounded-lg',
  'transition-all duration-200 ease-in-out',
  'focus:outline-none focus:ring-2 focus:ring-offset-2',
  'disabled:opacity-50 disabled:cursor-not-allowed',
  variantStyles[variant],
  sizeStyles[size],
  className
);
---

{href ? (
  external ? (
    <a href={href} class={baseStyles} target="_blank" rel="noopener noreferrer">
      <slot />
    </a>
  ) : (
    <a href={href} class={baseStyles}>
      <slot />
    </a>
  )
) : (
  <button type={type} class={baseStyles} disabled={disabled}>
    <slot />
  </button>
)}
